# Exercise 3 — Modelling Neural Population Activity

**Releases on 2026-05-07.**

This directory is a placeholder until the exercise is released. On release day,
`starter.py` will appear here (Ex3 is code-only — no notebook) and this README
will be replaced with the full exercise brief.

## Topic (preview)

Population-level analysis of neural spiking: firing-rate matrices, raster
plots across trials and neurons, trial-to-trial variability.

## On release day

```bash
pixi run sync                  # pull the new exercise + data
pixi run start ex3             # create your working copy (ex3.py)
# ... edit ex3.py ...
pixi run submit ex3            # validate + push when done
```
