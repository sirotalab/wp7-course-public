[project]
name = "wp7-course-public"
description = "Student laptop bootstrap — installs datalad + git-annex for course data sync."
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64", "osx-64", "win-64"]

[dependencies]
datalad = ">=1.2,<2"
